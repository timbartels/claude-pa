#!/usr/bin/env python3
"""bin/pa-dashboard — entry point for the live PA dashboard frame renderer."""

from __future__ import annotations

import os
import sys
from pathlib import Path

_lib = Path(__file__).resolve().parents[1] / "lib"
sys.path.insert(0, str(_lib))

from pa.dashboard_render import main  # noqa: E402

if __name__ == "__main__":
    main()
